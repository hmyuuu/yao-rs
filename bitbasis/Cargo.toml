[package]
name = "bitbasis"
version.workspace = true
edition.workspace = true

[dependencies]
num-complex = "0.4"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
